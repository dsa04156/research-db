# 주간 AI 인프라 심층 보고서 — 2026-09-08

## 범위와 방법

2026-09-01~09-08의 self-evolving harness, AI agents, Kubernetes, cloud/distributed systems, edge AI를 조사했다. X/Twitter는 제외했다. 공식 문서·릴리스와 독립 기술 자료를 우선했고, 단일 사전논문과 SNS 신호는 핵심 결론에서 제외했다.

## 검증된 변화

### 1. 하네스가 평가·통제 계층으로 이동

에이전트의 운영 전환에서 자동 평가, 결함 유형화, 실행 추적, 승인 경계가 공통 과제로 나타났다. 이는 단순 프롬프트 조정이 아니라 실행 과정을 재현하고 실패를 분류하는 운영 계층의 문제다. [src_005] [src_006] [src_011]

### 2. Kubernetes 에이전트의 권한·복구 경계 강화

Kubernetes v1.37은 노드 구성요소를 비-root 사용자 네임스페이스에서 실행하는 기능을 베타로 올렸다. kagent v0.10.0은 영속 세션 저장, 소유자 범위 task 접근, 샌드박스 런타임과 배포 제어를 강화했다. Red Hat 참조 구조는 이미지 스캔 API를 CI 병목이 되지 않게 고가용성으로 구성한다. [src_001] [src_002] [src_003]

### 3. 서비스별 신호를 읽는 자동화

Flink의 상태ful job 자동확장과 RHACS scanning의 HA 구조는 단순 CPU 임계치보다 작업·체크포인트·CI 경로의 운영 의미를 반영해야 함을 보여준다. 다만 서로 다른 워크로드이므로 하나의 보편적 성능 주장으로 일반화하지 않는다. [src_003] [src_004]

## 반증 검색과 트렌드 품질

고위험 수치마다 독립 재현을 찾았지만 확인하지 못했다. 상세 미확정 목록은 검증기가 생성한 별도 JSON에만 보존했다. discovery 결과는 후보 원문과 무관한 인기 영상이 같은 군집에 섞여 “다중 플랫폼 확인”으로 렌더링됐으므로 핵심 결론에 사용하지 않았다.

## 운영 권고

에이전트 실행 계층에는 최소권한, 세션 내구성, tool effect 멱등성, 승인 로그, 장애 후 재개 시험을 포함한다. Kubernetes 적용 전에는 rootless 호환 CNI·CSI를 별도 노드 풀에서 검증한다. 자동확장은 리소스 사용률 외에 queue, checkpoint, cache, CI critical path를 관측한다.
